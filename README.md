# Simple-Translator
Enter you rapid API key in the server.js and start translating.
To build the Docker image run:
docker build -t simple-translator .
Then run: docker-compose up -d
The site is accessible via:
http://localhost:3000/